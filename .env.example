# For Railway: Add PostgreSQL plugin - DATABASE_URL is set automatically
# For local dev: Use a free Postgres (Neon, Supabase, etc.) or run Postgres locally

DATABASE_URL="postgresql://user:password@host:5432/dbname"
